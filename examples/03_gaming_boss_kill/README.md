# Example 3: Gaming Boss Encounter & Guild Notifications

This example demonstrates parallel child branch execution where a victory condition triggers both reward projection and external Discord webhook sinks simultaneously.

### Running the Example:

```bash
cd examples/03_gaming_boss_kill
go run main.go
```
